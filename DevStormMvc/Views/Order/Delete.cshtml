@model DevStormMvc.Models.OrderModel

@{
    ViewBag.Title = "Delete";
}

<div class="container">
    <div class="gap gap-small"></div>
    <div class="row row-col-gap">
        <div class="text-center">
            <h1>Are you sure you want to delete this order ?</h1>
            <div>

                <hr />
              
                    <div class="_box-highlight">
                        <h3>Product : @Html.DisplayFor(model => model.ProductName)</h3>
                        <h3>Quantity : @Html.DisplayFor(model => model.Quantity)</h3>
                        <h3>Price : @Html.DisplayFor(model => model.Price)</h3>
                     
                        <div class="gap gap-small"></div>
                        @*<ul class="product-page-actions-list">
                            <li>@Html.ActionLink("Edit", "Edit", new { id = Model.productId }, htmlAttributes: new { @class = "btn btn-warning", @type = "submit" })</li>
                            <li>@Html.ActionLink("Back to List", "Index", "", htmlAttributes: new { @class = "btn btn-info", @type = "submit" })</li>
                        </ul>*@
                        <div class="gap gap-small"></div>
                    </div>
                    

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-danger" /> |
                        @*@Html.ActionLink("Back to List", "Index")*@
                        
                        @Html.ActionLink("Back to List", "Index", "", htmlAttributes: new { @class = "btn btn-warning", @type = "submit" })
                    </div>
                }
            </div>
            </div>
        </div>
</div>